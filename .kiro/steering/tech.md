# Technology Stack

このプロジェクトはまだ実装前です。2026-06-24時点で `package.json` と `src/` は
存在しないため、以下は今後の実装で守る技術方針です。実際の依存関係を追加する前に、
仕様と設計で確認してください。

## Architecture

ブラウザ上で動くクライアントサイドゲームとして設計します。

主要な責務は分離します。

- カメラ入力: ブラウザのカメラ権限と映像ストリーム管理
- 姿勢検出: ライブラリ初期化、モデル読み込み、ランドマーク取得
- 表示: アバター、壁、UI、結果画面の描画
- ゲーム進行: 状態遷移、タイマー、レベル、ライフ、コンボ
- 判定: 壁パターンと姿勢ランドマークの衝突判定
- スコア: 成功、失敗、ボーナス、最終結果の計算

外部APIやブラウザAPIに依存する処理と、純粋なゲームロジックを分けます。
判定やスコアは単体でテストしやすい形にしてください。

## Candidate Technologies

最終決定は仕様・設計フェーズで行いますが、現時点の有力候補は次の通りです。

- Language: TypeScript
- Frontend: React
- Build Tool: Vite
- Pose Detection: MediaPipe Tasks Visionなどのブラウザ対応姿勢検出ライブラリ
- Rendering: Canvas 2D
- Styling: CSSまたはReactアプリに適したスタイリング方式

参考リポジトリと同じ技術名が候補に入ることは許容します。ただし、コード、設定、
構成、実装詳細はコピーしません。

## Development Environment

- SSH接続したUbuntuサーバー上で開発する。
- ブラウザでのカメラ利用は、基本的に `localhost` または安全なコンテキストを前提にする。
- 別端末から動作確認する場合は、開発サーバーのhost設定、HTTPS要件、カメラ権限を
  事前に確認する。
- Codexを使う。Claude Code専用の前提やファイルは導入しない。

## Development Standards

### Type Safety

- TypeScriptを採用する場合は、strictな型付けを基本にする。
- 姿勢ランドマーク、壁パターン、ゲーム状態、判定結果は型で表現する。
- `any` は外部ライブラリ境界など避けられない場合に限定し、理由が分かる形にする。

### Runtime State

- カメラ開始前に重い姿勢検出モデルを初期化しない。
- ユーザー操作を起点に、カメラ権限、姿勢検出、描画、ゲーム開始を順に進める。
- 停止、リトライ、ページ遷移でカメラストリームやアニメーションループを解放する。

### Error Handling

- カメラ許可拒否
- カメラ非搭載または取得失敗
- 姿勢検出モデルの読み込み失敗
- ブラウザ非対応
- 姿勢が検出できない状態

これらはゲーム不能状態として扱い、ユーザーに次の行動が分かるUIを用意します。

### Testing

- 衝突判定、スコア計算、レベル進行は純粋関数としてテスト可能にする。
- カメラや姿勢検出の実機依存部分は、薄いアダプターに閉じ込める。
- 実装後は少なくとも型チェックとビルドを実行し、結果を報告する。

## Commands

現時点では `package.json` がないため、標準コマンドは未定です。
実装でプロジェクトを初期化したら、READMEと本ファイルに次を明記してください。

```bash
# 開発サーバー
# ビルド
# 型チェック
# テスト
```

## Key Technical Decisions

- 姿勢検出結果を直接UI全体に流さず、ゲームで必要な形に変換して扱う。
- 壁パターン、判定、スコアはデータとロジックを分け、調整しやすくする。
- 描画は毎フレーム更新されるため、Reactの通常レンダリングとCanvas描画の責務を混ぜすぎない。
- 参考リポジトリからのコピーではなく、仕様に基づいた独自実装を行う。
